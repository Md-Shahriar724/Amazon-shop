
import PropTypes from 'prop-types';

const Product = () => {
    return (
        <div>
            <h1>Product route</h1>
        </div>
    );
};

Product.propTypes = {
    
};

export default Product;